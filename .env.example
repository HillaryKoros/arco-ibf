# ── Database ──
DB_PASSWORD=e4drr_dev

# ── Django / Wagtail CMS ──
DJANGO_SETTINGS_MODULE=disasterevents_cms.settings.dev
DJANGO_SECRET_KEY=change-me-in-production

# ── Frontend (Next.js uses these at build time) ──
# Internal Docker network URLs — used by Next.js rewrites
CMS_API_URL=http://cms:8000
GEO_API_URL=http://geoapi:8080
TITILER_API_URL=http://titiler:8080
